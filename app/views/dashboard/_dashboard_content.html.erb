
<main class="py-4">
    <%= tab_panel active: current_view == 'grid' do %>
        <%= render 'dashboard/grid', files: files, folders: folders, current_folder: current_folder %>
    <% end %>

    <%= tab_panel active: current_view == 'list' do %>
        <%= render 'dashboard/table', files: files, folders: folders, current_folder: current_folder %>
    <% end %>
</main>
